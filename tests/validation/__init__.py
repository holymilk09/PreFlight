"""Validation tests using real document datasets.

These tests validate algorithm accuracy using public document datasets
like FUNSD, CORD, and DocLayNet instead of synthetic fixtures.

Run with: pytest tests/validation/ -v

Note: Requires 'datasets' package: pip install datasets
"""
